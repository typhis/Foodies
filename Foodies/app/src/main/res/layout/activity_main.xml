<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.zirakai.foodies.MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="@string/app_name"
        android:id="@+id/Foodies"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <!-- PhoneNumber -->
    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="phone"
        android:ems="10"
        android:id="@+id/PhoneNumber"
        android:hint="Enter Your Phone Number"
        android:layout_above="@+id/Password"
        android:layout_centerHorizontal="true" />

    <!-- Password -->
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/Password"
        android:inputType="textPassword"
        android:hint="Enter your Password"
        android:textAlignment="center"
        android:layout_above="@+id/Login"
        android:layout_marginBottom="23dp"
        android:layout_alignLeft="@+id/PhoneNumber"
        android:layout_alignStart="@+id/PhoneNumber"
        android:layout_alignRight="@+id/PhoneNumber"
        android:layout_alignEnd="@+id/PhoneNumber" />

    <!-- Button Login -->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/login"
        android:id="@+id/Login"
        android:layout_alignTop="@+id/Register"
        android:layout_toLeftOf="@+id/Foodies"
        android:layout_toStartOf="@+id/Foodies" />

    <!-- Button Register -->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/register"
        android:id="@+id/Register"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@+id/Foodies"
        android:layout_toEndOf="@+id/Foodies" />

</RelativeLayout>
